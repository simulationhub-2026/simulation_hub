# دليل نشر موقع Simulation Hub على GitHub

> [!IMPORTANT]
> لقد قمت بضبط التطبيق ليعمل على **عنوان فرعي (Sub-path)** مثل: `https://username.github.io/simulation_hub/`. 
> إذا قمت بتغيير اسم المستودع (Repository Name) على GitHub ليكون مختلفاً عن `simulation_hub` فيجب عليك تعديل الاسم في مكانين:
> 1. ملف `vite.config.js`: في خانة `base`.
> 2. ملف `src/App.jsx`: في خانة `basename`.

لنشر مشروعك على GitHub ومشاركته مع العالم، اتبع الخطوات التالية:

## 1. إنشاء مستودع (Repository) جديد
1. اذهب إلى [GitHub](https://github.com) وسجل دخولك.
2. اضغط على زر **"+"** في أعلى الصفحة واختر **New repository**.
3. قم بتسمية المستودع (مثلاً: `simulation-hub`).
4. اختر **Public** إذا كنت تريد أن يراه الجميع.
5. لا تقم بإضافة README أو .gitignore (لأننا أعددناها بالفعل).
6. اضغط على **Create repository**.

---

## 💻 الطريقة الأسهل: استخدام GitHub Desktop
بما أن لديك تطبيق **GitHub Desktop**، يمكنك النشر بسهولة دون كتابة أوامر:

1. افتح تطبيق **GitHub Desktop**.
2. اختر **File** من القائمة العلوية، ثم **Add Local Repository**.
3. قم باختيار مجلد المشروع: `d:\PHD\mat.simulation.hub@gmail.com\simulation_hub`.
4. إذا لم يكن هناك مستودع فعلي سيظهر لك خيار "create a repository here"، اضغط عليه.
5. في الجهة اليسرى، ستجد قائمة بالملفات، اكتب في الأسفل (Summary) أي وصف (مثلاً: `Simulation Hub Initial Commit`).
6. اضغط على الزر الأزرق: **Commit to main**.
7. في الأعلى، اضغط على **Publish repository** (أو **Push origin**).
8. تأكد من إزالة العلامة عن "Keep this code private" إذا كنت تريده متاحاً للعامة.
9. اضغط **Publish**.

تم! مشروعك الآن على GitHub.

---

## ⌨️ الطريقة البديلة: استخدام الطرفية (Terminal)
إذا أردت استخدام الأوامر اليدوية (Powershell/CMD):

```powershell
# أولاً: قم بتعريف نفسك لـ Git (مرة واحدة فقط)
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"

# إضافة الملفات
git add .

# أول Commit
git commit -m "Initial commit - Simulation Hub complete"

# تغيير اسم الفرع الرئيسي إلى main
git branch -M main

# ربط المستودع المحلي بـ GitHub
# استبدل USERNAME و REPO_NAME ببياناتك
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# رفع الملفات
git push -u origin main
```

## 3. النشر المجاني (GitHub Pages)
إذا أردت أن يعمل الموقع كـ رابط حقيقي (Live Link):
1. قم بتثبيت حزمة gh-pages:
   ```bash
   npm install gh-pages --save-dev
   ```
2. أضف هذه السطر في ملف `package.json` قبل "dependencies":
   `"homepage": "https://USERNAME.github.io/REPO_NAME",`
3. في قسم "scripts" أضف:
   `"predeploy": "npm run build",`
   `"deploy": "gh-pages -d dist"`
4. ثم قم بتشغيل:
   ```bash
   npm run deploy
   ```

سيكون موقعك متاحاً على الرابط الذي وضعته في `homepage`.

---

### 💡 نصيحة احترافية (Vercel أو Netlify)
إذا وجدت صعوبة في GitHub Pages، أنصحك باستخدام [Vercel](https://vercel.com) أو [Netlify](https://www.netlify.com).
كل ما عليك فعله هو ربط حساب GitHub الخاص بك، واختيار المستودع، وسيتم النشر تلقائياً وبسهولة فائقة في ثوانٍ.
